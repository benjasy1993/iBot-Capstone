const mocha = require('mocha');
const assert = require('assert');

mocha.describe('Jira interaction', function () {
    mocha.it('should create a task', function (done) {
        assert(true);
        done();
    });
});